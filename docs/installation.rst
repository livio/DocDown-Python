.. highlight:: shell

============
Installation
============


Stable release
--------------

To install DocDown Python, run this command in your terminal:

.. code-block:: console

    $ pip install docdown_python

This is the preferred method to install DocDown Python, as it will always install the most recent stable release. 

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for DocDown Python can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/Mobelux/DocDown-Python

Or download the `tarball`_:

.. code-block:: console

    $ curl  -OL https://github.com/Mobelux/DocDown-Python/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/Mobelux/DocDown-Python
.. _tarball: https://github.com/Mobelux/DocDown-Python/tarball/master
